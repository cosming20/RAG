[project]
name = "rag-agent-validation"
version = "0.1.0"
description = "Validation Agent — hallucination check, source attribution"
requires-python = ">=3.12"
dependencies = ["rag-common"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["validation_agent"]

[tool.uv.sources]
rag-common = { workspace = true }
